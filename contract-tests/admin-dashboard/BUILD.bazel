load("//tools/contract_test:defs.bzl", "consumer_contract_test")

consumer_contract_test(
    name = "contract_tests",
    stubs_jar = "//contract-tests:artifacts/blitz-pay-stubs.jar",
    wiremock_port = "8089",
    # Runs `npm run test:contract` inside the Angular submodule.
    # Add a test:contract script to blitzpay-admin-dashboard/package.json
    # that points HTTP calls at CONTRACT_TEST_BASE_URL.
    test_cmd = [
        "npm",
        "--prefix",
        "blitzpay-admin-dashboard",
        "run",
        "test:contract",
    ],
)
