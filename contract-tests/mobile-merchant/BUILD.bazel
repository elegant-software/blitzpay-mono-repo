load("//tools/contract_test:defs.bzl", "consumer_contract_test")

consumer_contract_test(
    name = "contract_tests",
    stubs_jar = "com.elegant.software.blitzpay:blitz-pay:jar:stubs:0.2.2",
    app_files = "//:mobile_merchant_files",
    wiremock_port = "8091",
    # Runs `npm run test:contract` inside the merchant mobile submodule.
    # Add a test:contract script to blitz-pay-prototype/blitz-pay-merchant/package.json
    # that points HTTP calls at CONTRACT_TEST_BASE_URL.
    test_cmd = [
        "npm",
        "--prefix",
        "blitz-pay-prototype/blitz-pay-merchant",
        "run",
        "test:contract",
    ],
)
